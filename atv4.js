let menuPrincipal = ['pizza', 'hamburguer', 'costela', 'batata frita'];
let menuDeSobremesas = ['chocolate', 'sorvete', 'bolo', 'pudim'];

let menuCompleto = menuPrincipal.concat(menuDeSobremesas);

console.log(menuCompleto);